protocolName: themis
protocolPath: /home/cb/shadow-experiments/themis/
executionDir: /home/cb/shadow-experiments/themis/ # directory to put Shadow YAML file
experimentsDirectory: /home/cb/myShadowExperiments/themis-10ms3 # USE AN ABSOLUTE PATH
experiments:
  - 4rep: # this will be passed to as data_dir value in shadow.yaml
      misc:
        runahead: 630 us
        duration: 32 s
        clientDelay: 0 s # timestamp to start CLIs
        parallelism: 16
        useShortestPath: false
      network:
        bandwidthUp: 10 Gibit # does not apply to clients (for now)
        bandwidthDown: 10 Gibit # does not apply to clients (for now)
        latency:
          uniform: true
          delay: 10000 us
      replica: # should be validated by connectors (currently not done, they are passed as-is)
        replicas: 4
        minBatchSize: 400
        maxBatchSize: 400
        replySize: 0
        batchReplies: true
        peerAuthentication: Ed25519
        clientAuthentication: Hmac
        timeout:
          secs: 4
          nano: 100_000_000
      client:
        clients: 16
        payload: 0
        concurrent: 175
        duration: 30 # WATCH OUT FOR THIS VALUE (for now)
  - 8rep: # this will be passed to as data_dir value in shadow.yaml
      misc:
        runahead: 630 us
        duration: 32 s
        clientDelay: 0 s # timestamp to start CLIs
        parallelism: 16
        useShortestPath: false
      network:
        bandwidthUp: 10 Gibit # does not apply to clients (for now)
        bandwidthDown: 10 Gibit # does not apply to clients (for now)
        latency:
          uniform: true
          delay: 10000 us
      replica: # should be validated by connectors (currently not done, they are passed as-is)
        replicas: 8
        minBatchSize: 400
        maxBatchSize: 400
        replySize: 0
        batchReplies: true
        peerAuthentication: Ed25519
        clientAuthentication: Hmac
        timeout:
          secs: 4
          nano: 100_000_000
      client:
        clients: 16
        payload: 0
        concurrent: 175
        duration: 30 # WATCH OUT FOR THIS VALUE (for now)
  - 16rep: # this will be passed to as data_dir value in shadow.yaml
      misc:
        runahead: 630 us
        duration: 32 s
        clientDelay: 0 s # timestamp to start CLIs
        parallelism: 16
        useShortestPath: false
      network:
        bandwidthUp: 10 Gibit # does not apply to clients (for now)
        bandwidthDown: 10 Gibit # does not apply to clients (for now)
        latency:
          uniform: true
          delay: 10000 us
      replica: # should be validated by connectors (currently not done, they are passed as-is)
        replicas: 16
        minBatchSize: 400
        maxBatchSize: 400
        replySize: 0
        batchReplies: true
        peerAuthentication: Ed25519
        clientAuthentication: Hmac
        timeout:
          secs: 4
          nano: 100_000_000
      client:
        clients: 16
        payload: 0
        concurrent: 175
        duration: 30 # WATCH OUT FOR THIS VALUE (for now)
  - 32rep: # this will be passed to as data_dir value in shadow.yaml
      misc:
        runahead: 630 us
        duration: 32 s
        clientDelay: 0 s # timestamp to start CLIs
        parallelism: 16
        useShortestPath: false
      network:
        bandwidthUp: 10 Gibit # does not apply to clients (for now)
        bandwidthDown: 10 Gibit # does not apply to clients (for now)
        latency:
          uniform: true
          delay: 10000 us
      replica: # should be validated by connectors (currently not done, they are passed as-is)
        replicas: 32
        minBatchSize: 400
        maxBatchSize: 400
        replySize: 0
        batchReplies: true
        peerAuthentication: Ed25519
        clientAuthentication: Hmac
        timeout:
          secs: 4
          nano: 100_000_000
      client:
        clients: 16
        payload: 0
        concurrent: 175
        duration: 30 # WATCH OUT FOR THIS VALUE (for now)
  - 64rep: # this will be passed to as data_dir value in shadow.yaml
      misc:
        runahead: 630 us
        duration: 32 s
        clientDelay: 0 s # timestamp to start CLIs
        parallelism: 16
        useShortestPath: false
      network:
        bandwidthUp: 10 Gibit # does not apply to clients (for now)
        bandwidthDown: 10 Gibit # does not apply to clients (for now)
        latency:
          uniform: true
          delay: 10000 us
      replica: # should be validated by connectors (currently not done, they are passed as-is)
        replicas: 64
        minBatchSize: 400
        maxBatchSize: 400
        replySize: 0
        batchReplies: true
        peerAuthentication: Ed25519
        clientAuthentication: Hmac
        timeout:
          secs: 4
          nano: 100_000_000
      client:
        clients: 16
        payload: 0
        concurrent: 100
        duration: 30 # WATCH OUT FOR THIS VALUE (for now
  - 128rep: # this will be passed to as data_dir value in shadow.yaml
       misc:
         runahead: 630 us
         duration: 32 s
         clientDelay: 0 s # timestamp to start CLIs
         parallelism: 16
         useShortestPath: false
       network:
         bandwidthUp: 10 Gibit # does not apply to clients (for now)
         bandwidthDown: 10 Gibit # does not apply to clients (for now)
         latency:
           uniform: true
           delay: 10000 us
       replica: # should be validated by connectors (currently not done, they are passed as-is)
         replicas: 128
         minBatchSize: 400
         maxBatchSize: 400
         replySize: 0
         batchReplies: true
         peerAuthentication: Ed25519
         clientAuthentication: Hmac
         timeout:
           secs: 4
           nano: 100_000_000
       client:
         clients: 16
         payload: 0
         concurrent: 50
         duration: 30 # WATCH OUT FOR THIS VALUE (for now)
